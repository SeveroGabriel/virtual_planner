# Referencia para o editor de variaveis da Railway. NAO executar com source.
PORT=8080
# So runtime do nginx; sem barra final e sem /api. "backend" e o servico API.
BACKEND_URL=http://${{backend.RAILWAY_PRIVATE_DOMAIN}}:${{backend.PORT}}
# Build do Vite; nunca use o dominio privado em VITE_*.
VITE_API_URL=/api
